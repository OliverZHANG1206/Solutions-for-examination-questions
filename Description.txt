任务7 关于UART通讯的问题：
由于在题目中未标明UART通讯模式，因此在这里使用的是UART中断接收，而非阻塞式和DMA。

任务8 关于USB的通讯问题：
第一个文件夹内只能实现发送并返回数据，但是并不能满足STM32主动通讯。
第二个文件夹内 由于CDC_Transmit_FS为静态声明，因此单独写了一个封装函数，添加到头文件并能在main.c内直接引用，因此可以实现STM32主动通过USB通讯。